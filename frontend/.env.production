# Baked into production builds so `npm run build` always targets the App Runner
# API (not the /api dev proxy). Not a secret. Update if the backend URL changes.
VITE_API_URL=https://3uphtjpwsh.us-east-1.awsapprunner.com
